<!-- AndroidManifest.xml — AppGastos con Quick Settings Tile + Overlay nativo. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos estándar. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- [OVERLAY] Permiso para que OverlayService pueda superponer UI sobre otras apps. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- [OVERLAY] OverlayService arranca como foreground (necesario en Android 14+). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="AppGastos"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|density"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- =================================== -->
        <!--  REGISTRO DEL QUICK SETTINGS TILE   -->
        <!-- =================================== -->
        <service
            android:name=".ExpenseTileService"
            android:label="Registrar gasto"
            android:icon="@mipmap/ic_launcher"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

        <!-- =================================== -->
        <!--  SERVICIO DE OVERLAY NATIVO         -->
        <!-- =================================== -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Overlay UI para registro rápido de gastos"/>
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

</manifest>
